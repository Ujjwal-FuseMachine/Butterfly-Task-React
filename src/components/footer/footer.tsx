import React, { ReactElement, ReactNode } from "react";

import "./Footer.scss";

const Footer = (): ReactElement => {
  const styles = {
    header: "footer-header",
  };
  return (
    <div className="footer">
      <div className="footer">
        <label>©2022 Butterfly.ai</label>
      </div>
    </div>
  );
};

export default Footer;
